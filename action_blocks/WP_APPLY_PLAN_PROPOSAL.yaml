# Action Spec: WP_APPLY_PLAN_PROPOSAL
action_type: WP_APPLY_PLAN_PROPOSAL
public_command_aliases: [apply_plan, commit_schedule]
contract: VALOR-contract-orch-wp
version: v1.0.1
purpose: Commit dates into WP/task truth from a governed Planning proposal while preserving the proposal-vs-commitment boundary.
allowed_modes: [EXECUTION]
side_effect_class: MUTATES_TRUTH
idempotent: false
confirmation_required: true
status: ACTIVE_PRE_FREEZE

required_payload_fields:
  - plan_proposal_id
  - date_updates
  - planning_provenance_stamps
  - calendar_logic_ref

optional_payload_fields:
  - change_reason
  - no_profile_override_stamp_set

target_fields:
  required: [wp_id]
  optional: []

result_schema_ref: schemas/objects/work_package_schema.json

validation_rules:
  - Only a PROPOSED plan generated by PLAN may be applied.
  - Applying a plan writes committed_start_date and committed_finish_date in WP truth.
  - planning_provenance_stamps must include preset/task_pool/profile or no-profile override stamp set/calendar/contract references used to generate the proposal.
  - calendar_logic_ref must include calendar_id, calendar_version, canonical_calendar_version, and timezone when the calendar is a wrapper.

notes:
  - Require explicit confirmation in orchestration before executing.
  - Refuse missing provenance stamps or unsupported calendar wrapper metadata.
